# Conversion engine
# PySide6 lives in the desktop repo's requirements only — it's not
# needed by the web container. The conversion_queue.py module that
# does `from PySide6.QtCore import ...` at the top isn't imported
# anywhere in `web/` (web/services/conversion.py uses its own
# ThreadPoolExecutor instead). SVG rendering used to go through
# QtSvg via image_handler._open_svg_via_qt; the web container now
# uses cairosvg for that path. Removing PySide6 saves ~200 MB
# off the image; adding cairosvg adds ~30 MB. Net: ~170 MB off.
Pillow>=10.0
striprtf>=0.0.26
psutil>=5.9
pdfminer.six>=20221105
# Pure-Python SVG → PNG renderer. Replaces the QtSvg path used in the
# desktop build. Requires libcairo2 at the OS level (installed by the
# Dockerfile's apt-get line).
cairosvg>=2.7

# Format-expansion deps (mirrors desktop repo)
pillow-avif-plugin>=1.4
pillow-heif>=0.16
pillow-jxl-plugin>=1.2
fonttools>=4.50
brotli>=1.1
py7zr>=0.21
zstandard>=0.22
pyarrow>=15.0
tomli-w>=1.0
PyYAML>=6.0
# Used by the Mandelbrot encoder + WAV/AIFF/FLAC encoder paths in
# masquerade.py (Stone PNG↔WAV, image↔audio crossings).
numpy>=1.26

# Web app
fastapi>=0.110
uvicorn[standard]>=0.27
sqlalchemy>=2.0
alembic>=1.13
argon2-cffi>=23.1
pyjwt>=2.8
authlib>=1.3
itsdangerous>=2.2
python-multipart>=0.0.9
jinja2>=3.1
slowapi>=0.1.9
httpx>=0.27
pydantic>=2.6
pydantic-settings>=2.2
cryptography>=42.0
email-validator>=2.1
aiosmtplib>=3.0

# Optional S3-compatible storage backend (AWS, MinIO, Backblaze B2,
# Cloudflare R2, etc.). Only imported when the operator flips
# storage_backend='s3'; local-only deployments don't load it. Adds
# roughly 30 MB to the image — kept in the main requirements file so
# the storage feature works out of the box rather than failing on
# first activation with "boto3 missing."
boto3>=1.34
